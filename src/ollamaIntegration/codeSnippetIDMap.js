// This object serves to match code snippets with their respective ids, so that hardcoding strings (magic strings) is counteracted. 
// This serves as an "enumeration." (JS by default doesn't have them). 
export const codeSnippetIDs = Object.freeze({
    sumTwoNumbers: "foo0",
    linearSearch: "foo1",
    numberOfEvenNumbers: "foo2",
    multiplyAnArrayOfNumbers: "foo3",
    insertionSortDescending: "foo4",
    returnAllFactorsOfANumber: "foo5"
});
